export interface Contacts {
    id: number,
    nome: string,
    telefone: string,
    email: string
}

export type UseContact = {
    nome: string,
    email: string,
    telefone: string,
    id: string
}